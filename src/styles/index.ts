import { createStitches } from '@stitches/react'

export const {
  config,
  styled,
  css,
  globalCss,
  keyframes,
  theme,
  createTheme
} = createStitches({
  theme: {
    colors: {
      rocketseat: '#8257e6'
    }
  }
})

